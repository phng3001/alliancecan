# Notes
* `--no-spliced-alignment`: Disable spliced alignment
* `--rna-strandness`: Specify strand-specific information (default is unstranded)
  - `RF` for Reverse Stranded
  - `FR` for Direct Stranded
